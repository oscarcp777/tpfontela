#include <iostream>
#include "Textura.h"
class Hidratar{

   private:
	public:

		Hidratar();
		static int hidratarEscenario(std::string atributos);
		static int hidratartextura(std::string atributos);
};
